host cr cr S" init-data at " type here hex. cr cr
target definitions

: init-data ( -- )
  [ host latest0 tliteral   var_latest tliteral target ] !
  [ host dp0     tliteral   var_dp     tliteral target ] ! ;

: init-hardware ( -- ) init-data init-serial ;

\ There's no shutdown hardware signal on the VersatilePB board!
\ This approximates it by sending "(bye) - C-a x to quit QEMU" and spinning.
: bye ( -- )
  cr cr
  ." *** (bye) ***" cr
  ." Type  C-a x  to quit QEMU" cr
  begin again ;
